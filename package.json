{
  "name": "socket-server",
  "version": "1.0.0",
  "description": "Socket server",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Socket.io",
    "Server"
  ],
  "dependencies": {
    "socket.io": "^4.6.1"
  }
}
